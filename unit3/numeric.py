'''
python has a rich set of built-in data types that enable
developers to handle a variety of data in a seamless and 
efficient way. These types are categorized into numeric, 
sequence, set, mapping, binary, and special types. let's explore them:
'''

#NUMERIC Types
    # 1.. Integers (int)
    # 2.. Floating (float)
    # 3.. Complex (complex)


# Whole numbers, positive or negative, with unlimited precision. 
a = 24
print(type(a))

# Numbers with a decimal point

y = 3.14
print(type(y))
#complex numbers Represented as a + bj, where a is the real part and b is the imaginary part.

z = 3+ 8j
print(z)
print(type(z)) #get type data 